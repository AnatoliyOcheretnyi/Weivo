import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  grid: {
    position: 'absolute',
    left: 0,
    right: 0,
    top: 12,
  },
  gridLine: {
    position: 'absolute',
    left: 0,
    right: 0,
    height: 1,
    backgroundColor: '#E5DBCF',
  },
});
