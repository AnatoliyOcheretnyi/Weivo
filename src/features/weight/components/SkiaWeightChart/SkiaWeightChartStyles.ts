import { StyleSheet } from 'react-native'
import { dimensions } from '@/theme/dimensions'
export const skiaWeightChartStyles = StyleSheet.create({
  canvas: {
    height: dimensions.chart.height,
    width: '100%',
  },
})
